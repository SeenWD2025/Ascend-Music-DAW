{
  "name": "@amg/tests",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AMG Music Platform Test Suite",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage",
    "test:unit": "vitest run --dir unit",
    "test:integration": "vitest run --dir integration",
    "test:rls": "vitest run --dir rls"
  },
  "devDependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "@types/node": "^20.10.0",
    "@types/uuid": "^10.0.0",
    "@vitest/coverage-v8": "^4.0.16",
    "@vitest/ui": "^4.0.16",
    "dotenv": "^16.3.1",
    "fastify": "^4.24.3",
    "vitest": "^4.0.16"
  },
  "dependencies": {
    "uuid": "^9.0.0"
  }
}
